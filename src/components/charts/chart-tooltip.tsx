export const chartTooltipStyle = {
  borderRadius: "var(--radius)",
  border: "1px solid var(--border)",
  background: "var(--card)",
  fontSize: 13,
} as const;

export const chartMargin = { top: 5, right: 10, left: -10, bottom: 5 } as const;

export const chartMarginRtl = { top: 5, right: -10, left: 10, bottom: 5 } as const;

export const chartAxisTick = { fontSize: 11 } as const;
